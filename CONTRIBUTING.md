# Contributing

RooMate Voiceへのコントリビューションを歓迎します。

## 開発フロー

1. Issueで変更目的と影響範囲を確認します。
2. `feature/...`または`fix/...`ブランチを作成します。
3. `npm install --ignore-scripts`後に変更します。
4. `npm run check`を通します。
5. PR本文に変更内容、確認方法、音声API課金を伴う手動テストの有無を記載します。

## コミット・PR

コミットメッセージとPR説明は日本語を推奨します。秘密情報、Discord Token、OpenAI API Key、音声サンプル、実運用ログはコミットしないでください。

## 音声Provider

第三者音声モデルを追加するPRでは、ライセンス、利用条件、話者同意の仕組み、AI音声表示、削除手順を明記してください。実在人物・著名人・既存キャラクターの無許諾クローンを前提とした実装は受け付けません。
